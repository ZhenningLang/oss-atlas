# kafka-tools

> 分类节点。Apache Kafka 客户端与管理界面。
> ← 返回[分类路由](../../INDEX.zh.md) · English: [INDEX.md](INDEX.md)

## 本分类项目

| 项目 | 何时用 | 页面 |
|---|---|---|
| **UI for Apache Kafka (provectus/kafka-ui)** | 一个免费、开源的 Web UI，用于管理和观测 Apache Kafka 集群——在浏览器里浏览 broker、topic、分区、消费组及其 lag，生产/查看消息，并接入 Schema Registry 和 Kafka Connect。**注意：** 活跃开发已转移到社区分叉 `kafbat/kafka-ui`（见健康度）。 | [→](kafka-ui.zh.md) |
| **kafka-python** | 一个纯 Python 的 Apache Kafka 客户端库——高层的 `KafkaConsumer`、`KafkaProducer`、`KafkaAdminClient` 类外加 CLI 脚本，没有 C/Cython/Rust 内核，所以在各种环境里安装都很简单。 | [→](kafka-python.zh.md) |

## 对比矩阵

| 选项 | 是否收录 | 一句话取舍 |
|---|---|---|
| [UI for Apache Kafka (provectus/kafka-ui)](kafka-ui.zh.md) | ✅ | 一个免费、开源的 Web UI，用于管理和观测 Apache Kafka 集群——在浏览器里浏览 broker、topic、分区、消费组及其 lag，生产/查看消息，并接入 Schema Registry 和 Kafka Connect。**注意：** 活跃开发已转移到社区分叉 `kafbat/kafka-ui`（见健康度）。 |
| [kafka-python](kafka-python.zh.md) | ✅ | 一个纯 Python 的 Apache Kafka 客户端库——高层的 `KafkaConsumer`、`KafkaProducer`、`KafkaAdminClient` 类外加 CLI 脚本，没有 C/Cython/Rust 内核，所以在各种环境里安装都很简单。 |
| (各页对比里点到的替代品) | 未收录 | 详见各页 Comparison。 |

## 什么该放这里

面向 **Apache Kafka** 的客户端与管理界面。通用消息库可能在 `task-queue`。
